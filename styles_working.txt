/* General Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Hero Section with Background Image */
.hero-section {
  position: relative;
  height: 100vh;
  background-image: url('diet.jpg'); /* Replace with any food-related image */
  background-size: cover;
  background-position: center;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.content {
  background: rgba(0, 0, 0, 0.5); /* Semi-transparent overlay */
  padding: 20px;
  border-radius: 10px;
}

h1 {
  font-size: 3rem;
  margin-bottom: 10px;
  color: #ffdd57;
}

p {
  font-size: 1.2rem;
  margin-bottom: 20px;
}

button {
  font-size: 1.1rem;
  padding: 10px 20px;
  color: #333;
  background-color: #ffdd57;
  border: none;
  border-radius: 5px;
  cursor: pointer;
  transition: background 0.3s ease;
}

button:hover {
  background-color: #ffc107;
}

/* Meal Plan Container */
.background-page {
  position: relative;
  height: 100vh;
  background-image: url('diet2.jpg');
  background-size: cover;
  background-position: center;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
}

.meal-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-gap: 20px;
  width: 100%;
  max-width: 1200px;
  margin: 0 auto;
  padding-top: 150px; /* Increase padding-top to move the boxes down */
}

.meal-day {
  background-color: rgba(0, 0, 0, 0.6);
  padding: 20px;
  border-radius: 10px;
  text-align: center;
  color: white;
  transition: transform 0.3s ease, box-shadow 0.3s ease;

  /* Adjust the width and height of the meal boxes */
  width: 400px; /* Set the desired width */
  height: 300px; /* Set the desired height */
}

.meal-day:hover {
  transform: scale(1.05);
  box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
}

h3 {
  margin-bottom: 15px;
  font-size: 3rem;
}

p {
  font-size: 1.8rem;
}

/* Recipe Box Text Style */
.recipe-container {
  font-size: 1rem; /* Default font size */
  padding: 40px; /* Increased padding for larger spacing */
  border-radius: 10px;
  background-color: rgba(0, 0, 0, 0.8);
  color: white;
  position: fixed; /* Fix position so it's on top */
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 9999;
  width: 600px; /* Larger width */
  height: auto; /* Allow height to adjust based on content */
}

.recipe-container h2 {
  font-size: 3rem; /* Larger font for the meal name */
  margin-bottom: 20px; /* Add space below the title */
}

.recipe-container h3 {
  font-size: 2rem; /* Slightly larger font for headings */
  margin-bottom: 15px; /* Space between the heading and the list */
}

.recipe-container ul {
  font-size: 1.5rem; /* Default font size for the ingredient and directions list */
  margin-bottom: 20px; /* Add space below the list */
}

.recipe-container button {
  margin-top: 20px; /* Space between directions and button */
  padding: 10px 20px;
  font-size: 1rem;
  cursor: pointer;
  border: none;
  background-color: #4CAF50; /* Green background */
  color: white;
  border-radius: 5px;
}

.recipe-container button:hover {
  background-color: #45a049; /* Darker green on hover */
}

